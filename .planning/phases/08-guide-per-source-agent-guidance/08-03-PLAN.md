---
phase: 8
plan: 08-03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - tests/test_intake_api.py
  - tests/test_bank_input_agent.py
  - tests/test_agent_entrypoint.py
  - api/routes/agent_instructions.py
autonomous: true
requirements: [GUIDE-01, GUIDE-02, GUIDE-03]
must_haves:
  truths:
    - "D-01 through D-14 are verified by tests or source assertions."
    - "D-12: Agent entrypoint and tests document that pending intake items include `guidance`."
    - "D-11 and D-13: Bank inputs remain separate and get `guidance: null` in agent queue only."
  artifacts:
    - path: "tests/test_intake_api.py"
      provides: "Backend guidance upload, update, detail, and lock tests."
    - path: "tests/test_bank_input_agent.py"
      provides: "Mixed queue uniform guidance field tests."
    - path: "tests/test_agent_entrypoint.py"
      provides: "Agent discoverability tests for guidance."
---

<objective>
Verify Phase 8 end to end and update agent-facing documentation.

This plan adds backend, mixed-queue, frontend build, and entrypoint checks so the
new guidance field is discoverable by agents and safe for users.
</objective>

<requirements_addressed>
- GUIDE-01: User can attach an agent message to an uploaded receipt/invoice.
- GUIDE-02: Agent message is visible to the agent when processing the intake item.
- GUIDE-03: Agent message is displayed on the intake detail page.
</requirements_addressed>

<must_haves>
<truths>
D-01 through D-14 from `08-CONTEXT.md` must be covered by tests, source assertions, or entrypoint documentation.
The entrypoint tells agents to consider item-level `guidance` from `/api/v1/agent/intake/pending`.
Tests prove bank items expose `guidance: null` without receiving bank guidance storage or detail UI.
</truths>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public entrypoint to external agent | Static onboarding docs tell agents how to interpret queue payloads. |
| Test database to migrations | Tests must exercise migrated schema and hydrated dataclasses. |
| Frontend build to runtime payload | TypeScript must agree with backend response fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | Entrypoint guidance docs | mitigate | Mention field semantics without source filenames, pending data, company state, or secrets. |
| T-08-09 | Tampering | Lock tests | mitigate | Add explicit `409` test for processed guidance update. |
| T-08-10 | Denial of Service | Test coverage gaps | mitigate | Run targeted backend tests and frontend build before marking phase complete. |
</threat_model>

<tasks>
<task id="T01" type="execute">
<title>Add backend guidance API tests</title>
<read_first>
- tests/test_intake_api.py
- api/routes/intake.py
- api/routes/agent.py
- services/intake.py
- repositories/intake_repo.py
- .planning/phases/08-guide-per-source-agent-guidance/08-01-PLAN.md
</read_first>
<files>
- tests/test_intake_api.py
</files>
<action>
Add tests proving guidance persistence, upload response, detail response, update endpoint behavior, queue exposure, and edit locking. Include one service-level test where `create_source_from_upload_content(..., agent_guidance="Book as travel")` returns and hydrates `agent_guidance`. Add route-level assertions that `upload_intake_source(..., agent_guidance="...")` returns `agent_guidance`, `get_intake_workspace_detail("voucher_source", ...)` returns `agent_guidance`, `list_pending_intake_sources` returns `guidance`, `update_intake_agent_guidance` updates a pending source, whitespace-only update clears to null, and a processed source update raises `HTTPException` status `409`.
</action>
<acceptance_criteria>
- `tests/test_intake_api.py` asserts stored `source.agent_guidance`.
- `tests/test_intake_api.py` asserts upload response key `agent_guidance`.
- `tests/test_intake_api.py` asserts detail response key `agent_guidance`.
- `tests/test_intake_api.py` asserts pending queue key `guidance`.
- `tests/test_intake_api.py` asserts guidance update status `409` for a processed source.
- `pytest tests/test_intake_api.py` exits 0.
</acceptance_criteria>
<verify>
<automated>pytest tests/test_intake_api.py</automated>
</verify>
</task>

<task id="T02" type="execute">
<title>Add mixed queue and entrypoint guidance checks</title>
<read_first>
- tests/test_bank_input_agent.py
- tests/test_agent_entrypoint.py
- api/routes/agent_instructions.py
- services/bank_inputs.py
- .planning/phases/08-guide-per-source-agent-guidance/08-RESEARCH.md
</read_first>
<files>
- tests/test_bank_input_agent.py
- tests/test_agent_entrypoint.py
- api/routes/agent_instructions.py
</files>
<action>
Update `api/routes/agent_instructions.py` so the startup sequence, pending intake endpoint docs, or guardrails state that pending intake items include optional item-level `guidance` and agents should consider it before posting. Preserve the public-safe contract: relative paths only and no company state. In `tests/test_bank_input_agent.py`, update `test_agent_pending_queue_returns_voucher_sources_and_bank_inputs` to assert voucher source `guidance` equals the uploaded guidance and bank item `guidance is None`. In `tests/test_agent_entrypoint.py`, assert serialized entrypoint text contains `guidance` and still excludes forbidden sensitive/company-state fields.
</action>
<acceptance_criteria>
- `api/routes/agent_instructions.py` contains `guidance` in the entrypoint payload context.
- `tests/test_bank_input_agent.py` asserts voucher-source queue guidance value.
- `tests/test_bank_input_agent.py` asserts bank queue guidance is `None`.
- `tests/test_agent_entrypoint.py` asserts `guidance` appears in serialized entrypoint response.
- `pytest tests/test_bank_input_agent.py tests/test_agent_entrypoint.py` exits 0.
</acceptance_criteria>
<verify>
<automated>pytest tests/test_bank_input_agent.py tests/test_agent_entrypoint.py</automated>
</verify>
</task>

<task id="T03" type="execute">
<title>Run integrated verification and source assertions</title>
<read_first>
- .planning/phases/08-guide-per-source-agent-guidance/08-01-PLAN.md
- .planning/phases/08-guide-per-source-agent-guidance/08-02-PLAN.md
- .planning/phases/08-guide-per-source-agent-guidance/08-03-PLAN.md
- package.json
- frontend-v3/package.json
</read_first>
<files>
- .planning/phases/08-guide-per-source-agent-guidance/08-03-SUMMARY.md
</files>
<action>
Run backend targeted tests `pytest tests/test_intake_api.py tests/test_bank_input_agent.py tests/test_agent_entrypoint.py`. Run frontend build from `frontend-v3` with `npm run build`. Run source assertions for `agent_guidance`, `agent-guidance`, `Meddelande till agent`, and `"guidance"` across backend and frontend files. Record command outcomes, changed files, deviations, and self-check result in `08-03-SUMMARY.md`.
</action>
<acceptance_criteria>
- Targeted backend pytest command exits 0.
- `cd frontend-v3 && npm run build` exits 0.
- Source assertions find `agent_guidance`, `agent-guidance`, `Meddelande till agent`, and `"guidance"` in expected files.
- `08-03-SUMMARY.md` contains `Self-Check: PASSED` only if all targeted commands pass.
</acceptance_criteria>
<verify>
<automated>pytest tests/test_intake_api.py tests/test_bank_input_agent.py tests/test_agent_entrypoint.py</automated>
<automated>cd frontend-v3 && npm run build</automated>
</verify>
</task>
</tasks>

<verification>
- Run `pytest tests/test_intake_api.py tests/test_bank_input_agent.py tests/test_agent_entrypoint.py`.
- Run `cd frontend-v3 && npm run build`.
- Run `rg 'agent_guidance|agent-guidance|Meddelande till agent|\"guidance\"' api services repositories domain db tests frontend-v3`.
</verification>

<success_criteria>
- Tests prove guidance upload, edit, lock, detail display data, and agent queue exposure.
- Entrypoint tells agents about item-level guidance without leaking sensitive state.
- Frontend build passes with updated API types and UI code.
- All Phase 8 requirements and context decisions are traceable to plans and verification.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | GUIDE-01 | Attach message to uploaded receipt/invoice | 08-03 | COVERED | Service/API tests.
REQ | GUIDE-02 | Agent sees message while processing | 08-03 | COVERED | Queue and entrypoint tests.
REQ | GUIDE-03 | Message displayed on detail page | 08-03 | COVERED | Detail API and frontend build/source assertions.
CONTEXT | D-01..D-14 | All locked decisions | 08-03 | COVERED | Final verification plan requires full traceability.
RESEARCH | Test strategy | Targeted backend and frontend build checks | 08-03 | COVERED | Commands are listed in verification.
</source_audit>

<output>
After completion, create `.planning/phases/08-guide-per-source-agent-guidance/08-03-SUMMARY.md`.
</output>
