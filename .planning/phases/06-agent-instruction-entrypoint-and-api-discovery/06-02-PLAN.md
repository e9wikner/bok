---
phase: 6
plan: 06-02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - api/routes/agent.py
  - tests/test_agent_entrypoint.py
  - README.md
autonomous: true
requirements: [AUTH-02, API-01, API-02, API-03, VER-01]
must_haves:
  truths:
    - "D-05: Ping is the first protected auth verification step after reading the public entrypoint."
    - "D-06: Remove `/api/v1/agent/keys/create`, `/api/v1/agent/keys`, and `/api/v1/agent/keys/{key_id}/revoke`."
    - "D-07: Remove `/api/v1/agent/spec/openapi` and `/api/v1/agent/spec/tools`."
    - "D-08: Remove `/api/v1/agent/operations/idempotent/{operation_id}`."
    - "D-10: `/openapi.json` is the truthful schema link; supported workflow endpoint list is authoritative."
    - "D-11: README technical references to removed routes are cleaned in Phase 6."
    - "D-12: Ping is the first protected auth verification step after reading the public entrypoint."
  artifacts:
    - path: "api/routes/agent.py"
      provides: "Dynamic ping and removed placeholder agent routes."
    - path: "tests/test_agent_entrypoint.py"
      provides: "Ping and removed-route behavior tests."
    - path: "README.md"
      provides: "Technical agent API reference cleaned for removed routes and new entrypoint."
  key_links:
    - source: "README.md"
      target: "/api/v1/agent-instructions/entrypoint"
      relation: "README points developers and agents to the supported startup entrypoint."
    - source: "api/routes/agent.py"
      target: "config.settings.api_version"
      relation: "Ping returns configured API version dynamically."
---

<objective>
Remove misleading placeholder agent routes, make authenticated ping dynamic and useful,
clean stale README references, and verify the supported agent startup contract.
</objective>

<requirements_addressed>
- AUTH-02: Placeholder API-key lifecycle endpoints are not presented as production key management.
- API-01: Agent ping returns current service, version, and authenticated actor data.
- API-02: Agent-facing schema/tool discovery points to truthful current endpoints rather than stale placeholders.
- API-03: Agent startup flow links to existing supported workflow endpoints.
- VER-01: Backend tests verify ping behavior and route removal.
</requirements_addressed>

<must_haves>
<truths>
D-05: Ping is the first protected auth verification step after reading the public entrypoint.
D-06: Remove `/api/v1/agent/keys/create`, `/api/v1/agent/keys`, and `/api/v1/agent/keys/{key_id}/revoke`.
D-07: Remove `/api/v1/agent/spec/openapi` and `/api/v1/agent/spec/tools`.
D-08: Remove `/api/v1/agent/operations/idempotent/{operation_id}`.
D-10: `/openapi.json` is the truthful schema link; supported workflow endpoint list is authoritative.
D-11: README technical references to removed routes are cleaned in Phase 6.
D-12: Ping is the first protected auth verification step after reading the public entrypoint.
</truths>
</must_haves>

<threat_model>
## Threat Model

### Threat: Existing clients or agents rely on fake placeholder routes
- **Severity:** medium
- **Mitigation:** Remove placeholder routes and expose unsupported/future features through the entrypoint.
- **Verification:** Tests assert removed placeholder routes return `404` or `405`, and README no longer references `/api/v1/agent/keys/*`.

### Threat: Ping leaks unnecessary internals
- **Severity:** low
- **Mitigation:** Ping returns only bounded `status`, `service`, `version`, `agent`, and `timestamp` fields.
- **Verification:** Tests assert ping has expected fields and no secret-like values.
</threat_model>

<tasks>
<task id="T01" type="execute">
<title>Remove placeholder agent routes</title>
<read_first>
- .planning/phases/06-agent-instruction-entrypoint-and-api-discovery/06-CONTEXT.md
- api/routes/agent.py
- tests/test_agent_entrypoint.py
</read_first>
<files>
- api/routes/agent.py
- tests/test_agent_entrypoint.py
</files>
<action>
Delete the placeholder route handlers in `api/routes/agent.py` for `/keys/create`, `/keys`, `/keys/{key_id}/revoke`, `/spec/openapi`, `/spec/tools`, and `/operations/idempotent/{operation_id}`. Remove imports that only supported those placeholders, including `uuid` and `hashlib` if unused. Extend tests to assert representative removed routes do not return successful placeholder responses.
</action>
<acceptance_criteria>
- `api/routes/agent.py` no longer contains route decorators for `/keys/create`, `/keys`, `/keys/{key_id}/revoke`, `/spec/openapi`, `/spec/tools`, or `/operations/idempotent/{operation_id}`.
- `api/routes/agent.py` no longer imports `uuid` or `hashlib` unless another remaining code path uses them.
- Tests assert `/api/v1/agent/spec/openapi`, `/api/v1/agent/spec/tools`, `/api/v1/agent/keys`, and `/api/v1/agent/operations/idempotent/test-op` return `404` or `405`.
</acceptance_criteria>
</task>

<task id="T02" type="execute">
<title>Make agent ping dynamic</title>
<read_first>
- api/routes/agent.py
- api/main.py
- config.py
- tests/test_agent_entrypoint.py
</read_first>
<files>
- api/routes/agent.py
- tests/test_agent_entrypoint.py
</files>
<action>
Update `POST /api/v1/agent/test/ping` to use `settings.api_version` for `version`, keep `service` stable as `bokfoering-api`, return the authenticated `actor` as `agent`, and return a current UTC ISO timestamp instead of the fixed `2026-03-21T10:00:00` value. Add tests that unauthenticated ping returns `401` and authenticated ping returns dynamic expected fields.
</action>
<acceptance_criteria>
- `api/routes/agent.py` imports `settings` from `config` or otherwise reads `settings.api_version`.
- `api/routes/agent.py` no longer contains the fixed ping timestamp string `2026-03-21T10:00:00`.
- Authenticated ping test posts to `/api/v1/agent/test/ping` with `auth_headers` and asserts `status == "ok"`, `service == "bokfoering-api"`, `version` is present, `agent == "api"`, and `timestamp` is present.
- Unauthenticated ping test posts to `/api/v1/agent/test/ping` without headers and asserts status code `401`.
</acceptance_criteria>
</task>

<task id="T03" type="execute">
<title>Clean README agent API references</title>
<read_first>
- README.md
- .planning/phases/06-agent-instruction-entrypoint-and-api-discovery/06-CONTEXT.md
</read_first>
<files>
- README.md
</files>
<action>
Update the README agent-integration section so it references `GET /api/v1/agent-instructions/entrypoint` as the agent startup entrypoint and removes stale text that says `/api/v1/agent/keys/*` exists but is not persistent. Keep the broader OpenClaw deployment instructions out of this phase.
</action>
<acceptance_criteria>
- `README.md` contains `/api/v1/agent-instructions/entrypoint`.
- `README.md` does not contain `/api/v1/agent/keys/*`.
- `README.md` still mentions `BOKFOERING_API_KEY` as the supported agent credential.
</acceptance_criteria>
</task>

<task id="T04" type="execute">
<title>Run focused verification</title>
<read_first>
- tests/test_agent_entrypoint.py
- tests/test_agent_instructions_separation.py
- tests/test_agent_accounting_workflow.py
- README.md
</read_first>
<files>
- tests/test_agent_entrypoint.py
- README.md
</files>
<action>
Run the focused backend tests for the entrypoint, agent instructions, and agent accounting workflow. If failures are caused by the route removals or ping changes, fix the implementation or tests within this plan's file scope. Do not broaden into Phase 7 deployment documentation.
</action>
<acceptance_criteria>
- `python -m pytest tests/test_agent_entrypoint.py tests/test_agent_instructions_separation.py tests/test_agent_accounting_workflow.py` exits 0.
- `rg "/api/v1/agent/keys|/api/v1/agent/spec|/api/v1/agent/operations/idempotent" README.md api/routes tests` returns no README references and no remaining route decorators for removed placeholder endpoints.
- `rg "/api/v1/agent-instructions/entrypoint" README.md tests api/routes/agent_instructions.py` finds the entrypoint in all three areas.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `python -m pytest tests/test_agent_entrypoint.py tests/test_agent_instructions_separation.py tests/test_agent_accounting_workflow.py`.
- Run `rg "/api/v1/agent/keys|/api/v1/agent/spec|/api/v1/agent/operations/idempotent" README.md api/routes tests`.
- Run `rg "/api/v1/agent-instructions/entrypoint" README.md tests api/routes/agent_instructions.py`.
</verification>

<success_criteria>
- Placeholder agent routes are removed.
- Ping returns dynamic, bounded auth verification data.
- README no longer documents removed key routes and now points to the entrypoint.
- Focused tests pass for entrypoint, existing instruction APIs, and agent workflow compatibility.
</success_criteria>
