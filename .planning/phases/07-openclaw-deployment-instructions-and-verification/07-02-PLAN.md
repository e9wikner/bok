---
phase: 7
plan: 07-02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tests/test_deployment_docs.py
autonomous: true
requirements: [AUTH-01, DOCS-01, DOCS-02, DOCS-03, DOCS-04, VER-02]
must_haves:
  truths:
    - "D-01 through D-17: Documentation examples and setup claims are deterministically checked against source text and route/auth behavior."
    - "AUTH-01: Tests assert bearer auth wording and authenticated ping behavior."
    - "DOCS-01 through DOCS-04: Tests assert required setup section content, URL separation, curl examples, and starter instruction."
    - "VER-02: Tests check documented examples against actual health, entrypoint, and ping behavior."
  artifacts:
    - path: "tests/test_deployment_docs.py"
      provides: "Documentation source assertions plus in-process FastAPI route/auth checks."
  key_links:
    - source: "tests/test_deployment_docs.py"
      target: "DEPLOYMENT.md"
      relation: "Documentation examples are asserted as source text."
    - source: "tests/test_deployment_docs.py"
      target: "api.main:app"
      relation: "Documented route paths and auth behavior are checked in-process."
---

<objective>
Add deterministic verification for Phase 7 deployment documentation examples.

Purpose: Prevent stale setup commands, wrong URL surfaces, leaked-secret examples,
and unsupported agent feature claims from entering the deployment guide.

Output: `tests/test_deployment_docs.py` with Markdown source assertions and
FastAPI/httpx route checks.
</objective>

<requirements_addressed>
- AUTH-01: Tests verify the documented credential is `Authorization: Bearer <BOKFOERING_API_KEY>` and ping uses bearer auth.
- DOCS-01: Tests verify `DEPLOYMENT.md` includes a post-deploy OpenClaw/HTTP-agent setup section.
- DOCS-02: Tests verify docs distinguish the human frontend URL from the backend API URL for agents on LAN.
- DOCS-03: Tests verify docs include concrete `curl` checks for health and authenticated agent ping.
- DOCS-04: Tests verify docs include a first setup-only OpenClaw instruction that delegates to the entrypoint.
- VER-02: Tests check documentation examples against actual route paths and auth behavior.
</requirements_addressed>

<must_haves>
<truths>
Documentation source assertions cover Phase 7 decisions D-01 through D-17.
Route tests prove documented health and entrypoint paths are public and documented ping is protected by bearer auth.
Tests do not require Docker, network access, curl execution, a browser, OpenClaw UI behavior, an AI evaluator, or an AI runtime.
</truths>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation to pytest | Tests parse owner-facing Markdown as a source of setup truth. |
| Test client to FastAPI app | In-process requests verify documented public/protected route behavior. |
| Secret placeholders to assertions | Tests verify examples use shell variables instead of repeated inline secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | `tests/test_deployment_docs.py` source assertions | mitigate | Assert docs use `API_KEY` and `Authorization: Bearer ${API_KEY}` examples, not hard-coded real key values. |
| T-07-06 | Spoofing | Documented ping behavior | mitigate | Test unauthenticated ping returns `401` and authenticated ping with `settings.api_key` returns `200`. |
| T-07-07 | Denial of Service | Verification command design | mitigate | Use in-process `httpx.ASGITransport`; do not start Docker, execute curl, or make network calls in pytest. |
| T-07-08 | Tampering | Unsupported feature claims | mitigate | Assert the OpenClaw setup docs do not present MCP, persistent key lifecycle, generated tool schemas, durable idempotency, model providers, or new backend routes as supported Phase 7 setup. |
</threat_model>

<tasks>
<task id="T01" type="execute">
<title>Create deployment documentation source assertions</title>
<name>Create deployment documentation source assertions</name>
<read_first>
- tests/test_deployment_docs.py
- DEPLOYMENT.md
- README.md
- .planning/phases/07-openclaw-deployment-instructions-and-verification/07-CONTEXT.md
- .planning/phases/07-openclaw-deployment-instructions-and-verification/07-AI-SPEC.md
- .planning/phases/07-openclaw-deployment-instructions-and-verification/07-PATTERNS.md
- tests/test_agent_entrypoint.py
</read_first>
<files>
- tests/test_deployment_docs.py
</files>
<action>
Create `tests/test_deployment_docs.py`. Read `DEPLOYMENT.md` with `pathlib.Path`. Add source assertions for the Phase 7 OpenClaw setup section: section placement after `### 9. Första inloggning` and before `## Uppdatera säkert på LAN`; required LAN URLs `http://SERVER_IP_OR_HOSTNAME:3000/login` and `http://SERVER_IP_OR_HOSTNAME:8000`; Docker-internal warning `BACKEND_URL=http://api:8000`; optional public HTTPS values `https://${API_DOMAIN}` and `https://${APP_DOMAIN}`; command examples with `BOK_API_URL`, `API_KEY`, `/health`, `/api/v1/agent-instructions/entrypoint`, `/api/v1/agent/test/ping`, and `Authorization: Bearer ${API_KEY}`; first OpenClaw instruction that is setup-only and delegates to the entrypoint; troubleshooting coverage for `401`, wrong URL/port, and Docker-internal URL confusion. Also assert no OpenClaw setup text presents MCP, persistent API-key lifecycle, generated tool-schema discovery, durable idempotency, model provider setup, frontend UI work, database schema changes, or new backend routes as supported Phase 7 behavior.
</action>
<acceptance_criteria>
- `tests/test_deployment_docs.py` imports `Path` from `pathlib` and reads `DEPLOYMENT.md`.
- Tests assert `DEPLOYMENT.md` ordering: first-login section before OpenClaw section before `## Uppdatera säkert på LAN`.
- Tests assert `BOK_API_URL`, `API_KEY`, `Authorization: Bearer ${API_KEY}`, `/health`, `/api/v1/agent-instructions/entrypoint`, and `/api/v1/agent/test/ping` exist in the OpenClaw setup section.
- Tests assert frontend/backend URL separation and `BACKEND_URL=http://api:8000` warning text exist.
- Tests assert troubleshooting covers `401`, wrong port or base URL, and Docker-internal URL confusion.
- Tests assert unsupported MCP/key lifecycle/generated tool schema/durable idempotency/model-provider claims are absent from the OpenClaw setup path.
</acceptance_criteria>
<done>
- `tests/test_deployment_docs.py` imports `Path` from `pathlib` and reads `DEPLOYMENT.md`.
- Tests assert `DEPLOYMENT.md` ordering: first-login section before OpenClaw section before `## Uppdatera säkert på LAN`.
- Tests assert `BOK_API_URL`, `API_KEY`, `Authorization: Bearer ${API_KEY}`, `/health`, `/api/v1/agent-instructions/entrypoint`, and `/api/v1/agent/test/ping` exist in the OpenClaw setup section.
- Tests assert frontend/backend URL separation and `BACKEND_URL=http://api:8000` warning text exist.
- Tests assert troubleshooting covers `401`, wrong port or base URL, and Docker-internal URL confusion.
- Tests assert unsupported MCP/key lifecycle/generated tool schema/durable idempotency/model-provider claims are absent from the OpenClaw setup path.
</done>
<verify>
<automated>python -m pytest tests/test_deployment_docs.py -q</automated>
</verify>
</task>

<task id="T02" type="execute">
<title>Verify documented route and auth behavior in-process</title>
<name>Verify documented route and auth behavior in-process</name>
<read_first>
- tests/test_deployment_docs.py
- tests/test_agent_entrypoint.py
- tests/conftest.py
- api/main.py
- api/routes/agent_instructions.py
- api/routes/agent.py
- api/deps.py
- config.py
- DEPLOYMENT.md
</read_first>
<files>
- tests/test_deployment_docs.py
</files>
<action>
Extend `tests/test_deployment_docs.py` with FastAPI in-process route checks using `httpx.ASGITransport(app=app)` and `httpx.AsyncClient`, matching the pattern in `tests/test_agent_entrypoint.py`. Verify `GET /health` returns `200`, `GET /api/v1/agent-instructions/entrypoint` returns `200` without auth, `POST /api/v1/agent/test/ping` returns `401` without auth, and `POST /api/v1/agent/test/ping` with `Authorization: Bearer {settings.api_key}` returns `200` with `status == "ok"` and `service == "bokfoering-api"`. Keep verification deterministic and local: do not execute curl, require Docker, use a live server, use network access, invoke OpenClaw, or add AI/model evaluation.
</action>
<acceptance_criteria>
- `tests/test_deployment_docs.py` imports `httpx`, `pytest`, `pytest_asyncio`, `app` from `api.main`, and `settings` from `config`.
- The health route test calls `/health` and asserts status code `200`.
- The entrypoint test calls `/api/v1/agent-instructions/entrypoint` without auth and asserts status code `200`.
- The ping auth test posts to `/api/v1/agent/test/ping` without auth and asserts status code `401`.
- The authenticated ping test posts to `/api/v1/agent/test/ping` with `Authorization: Bearer {settings.api_key}` and asserts status code `200`, `status == "ok"`, and `service == "bokfoering-api"`.
- `python -m pytest tests/test_deployment_docs.py tests/test_agent_entrypoint.py -q` exits 0.
</acceptance_criteria>
<done>
- `tests/test_deployment_docs.py` imports `httpx`, `pytest`, `pytest_asyncio`, `app` from `api.main`, and `settings` from `config`.
- The health route test calls `/health` and asserts status code `200`.
- The entrypoint test calls `/api/v1/agent-instructions/entrypoint` without auth and asserts status code `200`.
- The ping auth test posts to `/api/v1/agent/test/ping` without auth and asserts status code `401`.
- The authenticated ping test posts to `/api/v1/agent/test/ping` with `Authorization: Bearer {settings.api_key}` and asserts status code `200`, `status == "ok"`, and `service == "bokfoering-api"`.
- `python -m pytest tests/test_deployment_docs.py tests/test_agent_entrypoint.py -q` exits 0.
</done>
<verify>
<automated>python -m pytest tests/test_deployment_docs.py tests/test_agent_entrypoint.py -q</automated>
</verify>
</task>
</tasks>

<verification>
- Run `python -m pytest tests/test_deployment_docs.py tests/test_agent_entrypoint.py -q`.
- Run `rg "BOK_API_URL|API_KEY|Authorization: Bearer|/api/v1/agent-instructions/entrypoint|/api/v1/agent/test/ping" DEPLOYMENT.md tests/test_deployment_docs.py`.
- Run `rg "MCP|persistent|generated tool|durable idempotency|model provider" DEPLOYMENT.md tests/test_deployment_docs.py` and confirm any matches are only negative assertions or future/unsupported wording, not supported setup claims.
</verification>

<success_criteria>
- Documentation source assertions cover URL separation, command examples, starter instruction, troubleshooting, and unsupported-feature honesty.
- Route/auth tests prove the documented health, entrypoint, and ping examples match actual FastAPI behavior.
- Focused Phase 7 verification passes with pytest and without external services.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Document and verify post-deploy OpenClaw/HTTP-agent setup path | 07-01, 07-02 | COVERED | Docs in 07-01, deterministic checks in 07-02.
REQ | AUTH-01 | Docs and entrypoint explain `Authorization: Bearer <BOKFOERING_API_KEY>` | 07-01, 07-02 | COVERED | Tests assert docs and ping auth behavior.
REQ | DOCS-01 | `DEPLOYMENT.md` post-deploy setup section | 07-01, 07-02 | COVERED | Source assertions verify section placement/content.
REQ | DOCS-02 | Distinguish human frontend URL from agent backend API URL | 07-01, 07-02 | COVERED | Source assertions verify LAN and public HTTPS URL surfaces.
REQ | DOCS-03 | Curl checks for health and authenticated agent ping | 07-01, 07-02 | COVERED | Source assertions and route checks verify command paths/auth.
REQ | DOCS-04 | First agent prompt/instruction delegates to entrypoint | 07-01, 07-02 | COVERED | Source assertions verify setup-only starter instruction.
REQ | VER-02 | Documentation examples checked against actual route paths and auth behavior | 07-02 | COVERED | In-process FastAPI/httpx tests.
RESEARCH | AI-SPEC | No AI runtime, model calls, MCP, keys, or idempotency | 07-01, 07-02 | COVERED | Negative source assertions.
CONTEXT | D-01..D-17 | Locked Phase 7 setup, URL, prompt, and verification decisions | 07-01, 07-02 | COVERED | Implementation and verification are mapped to all decisions.
</source_audit>

<output>
After completion, create `.planning/phases/07-openclaw-deployment-instructions-and-verification/07-02-SUMMARY.md`.
</output>
