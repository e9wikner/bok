# Temporary HTTP-only config for initial Let's Encrypt certificate request
server {
    listen 80;
    server_name q.stefanwikner.se;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 503 "SSL certificate not yet provisioned";
    }
}
